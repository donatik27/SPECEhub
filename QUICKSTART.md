# 🚀 Швидкий старт

## За 5 хвилин до запуску

### 1️⃣ Встановіть залежності
```bash
pnpm install
```

### 2️⃣ Запустіть Docker (PostgreSQL + Redis)
```bash
docker-compose up -d
```

### 3️⃣ Налаштуйте базу даних
```bash
# Згенеруйте Prisma клієнт
pnpm db:generate

# Виконайте міграції
pnpm db:migrate

# (Опціонально) Заповніть тестовими даними
cd packages/database && pnpm prisma:seed && cd ../..
```

### 4️⃣ Запустіть всі сервіси
```bash
pnpm dev
```

### 5️⃣ Відкрийте браузер

- **UI**: http://localhost:3000
- **API Docs**: http://localhost:3001/api/docs
- **Health**: http://localhost:3001/health

---

## 🎯 Що отримаєте

✅ **Працюючий монорепо** з 3 сервісами  
✅ **PostgreSQL** з Prisma ORM  
✅ **Redis** для черг та кешу  
✅ **NestJS API** з Swagger документацією  
✅ **Next.js UI** з темною темою  
✅ **BullMQ Worker** з автоматичними job'ами  

---

## 📝 Корисні команди

```bash
# Подивитися логи Docker
docker-compose logs -f

# Відкрити Prisma Studio (GUI для БД)
pnpm db:studio

# Перезапустити все
docker-compose restart && pnpm dev
```

---

## ⚠️ Проблеми?

**Порт зайнятий?**
```bash
# Змініть порти в .env файлі
API_PORT=3002
# або для web - в package.json apps/web додайте -p 3001
```

**БД не підключається?**
```bash
# Перевірте статус
docker-compose ps

# Перезапустіть
docker-compose restart postgres
```

**Worker не стартує?**
```bash
# Перевірте Redis
docker-compose logs redis
```

---

## 🎓 Наступні кроки

1. Ознайомтесь з [повним README](./README.md)
2. Подивіться структуру в Prisma Studio: `pnpm db:studio`
3. Перегляньте API endpoints в Swagger: http://localhost:3001/api/docs
4. Почніть реалізацію Polymarket API клієнта (Phase 1)

**Готові до роботи? Давайте кодити! 🔥**

